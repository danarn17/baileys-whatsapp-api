/**
 * when a new QR is generated, ready for scanning
 * on (event: 'qr', listener: (qr: string) => void): this
 */
const qr = ({ db, share, connWA, connWS }) => async (qr) => {
}

module.exports = qr
