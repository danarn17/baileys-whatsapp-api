/**
 * when the connection has opened successfully
 * on (event: 'open', listener: (result: WAOpenResult) => void): this
 */
const open = ({ db, share, connWA, connWS }) => async (result) => {
}

module.exports = open
