/**
 * when the connection has been validated
 * on (event: 'connection-validated', listener: (user: WAUser) => void): this
 */
const connectionValidated = ({ db, share, connWA, connWS }) => async (user) => {
}

module.exports = connectionValidated
